---
import ButtonLink from './ButtonLink.astro'

interface Props {
  authHref: string
  catalogHref: string
}

const { authHref, catalogHref } = Astro.props
---

<section class="section cta" aria-labelledby="cta-title">
  <div>
    <p class="eyebrow">Готовы к прыжку на приключение?</p>
    <h2 id="cta-title">Откройте web-приложение и пройдите веселый сценарий аренды.</h2>
  </div>
  <div class="cta-actions">
    <ButtonLink href={authHref} variant="primary">Войти в приложение</ButtonLink>
    <ButtonLink href={catalogHref}>Сначала посмотреть мини-байки</ButtonLink>
  </div>
</section>
